// Quantidade de levels no jogo
const int max_levels  =  5 ;

// Altura e largura em pixels de cada tile/bloco
const int tile_size   = 32 ;

// Largura e altura do nosso mapa, em quantidade de blocos
const int map_tiles_x = 14 ;
const int map_tiles_y = 10 ;

// Onde o mapa começará a ser renderizado na tela
const int map_init_x = tile_size * (map_tiles_x) / 2 ;
const int map_init_y = tile_size * (map_tiles_y - 3) / 2 ;
